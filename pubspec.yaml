name: swiftconquer
description: Command & Conquer style RTS
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1

flutter:
  uses-material-design: true
